import React, { Component } from 'react';
import { DragDropContext, Droppable, Draggable } from 'react-beautiful-dnd';

const BlogTable = ({listBlog}) => (
    <p>BlogTable</p>
);

export default BlogTable;